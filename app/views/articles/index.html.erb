<div class="cards">
  <h1>Articles</h1>
  <ul>
    <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
      <% end %>
  </ul>
  <button type="button" class="btn btn-outline-primary"> <%= link_to "Add article", new_article_path %></button>
</div>
